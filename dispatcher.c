#include "hw2.h"
#include <stdio.h>

int run_dispatcher(const char *cmdfile_path)
{
    /* TODO: implement dispatcher logic in next steps */
    (void)cmdfile_path; /* avoid unused warning for now */
    return 0;
}
